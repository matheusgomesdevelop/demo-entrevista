import axios from "axios";

// export const hypnoboxUrl = `https://trisul-sa.hypnobox.com.br/email.receber.php`;
export const hypnoboxUrlChat = `https://trisul-sa.hypnobox.com.br`;

/*
export const POST_HYPNOBOX = (
  nome: string,
  email: string,
  id_produto: number,
  mensagem: string,
  ddd: string,
  tel_celular: string,
  midia: string
) => {
  return axios.get(
    `${hypnoboxUrl}?nome=${nome}&email=${email}&id_produto=${id_produto}&mensagem=${mensagem}&ddd_celular=${ddd}&tel_celular=${tel_celular}&midia=${midia}`
  );
};
*/
